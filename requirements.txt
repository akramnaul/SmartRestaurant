streamlit>=1.0.0
mysql-connector-python>=8.0.0 # For MySQL database connectivity (if using MySQL)
pymysql>=1.0.0                # Alternative MySQL connector (optional)
requests>=2.25.0              # For handling HTTP requests (if needed for authentication)
pandas>=1.3.0                 # For data manipulation (often used with Streamlit apps)
numpy>=1.21.0                 # For numerical operations (optional, if needed)

